/**
 * This package contains classes that handle GUI interactions.
 * The package also contains the model Package that maintains the state of the game, including the board, players, and game results.
 *
 *The main classes in this package are:
 * - BoardGameApplication: The entry point of the application. It extends the JavaFX Application class and handles the initialization of the application and scene switching.
 * - BoardGameController:  The controller class for the game board UI. It manages user interactions to the game board scene.
 * - BoardGameNamesController: The controller class for the player names UI. It handles the input of player names and switches to the game board UI when names are entered successfully.
 */
package boardgame;